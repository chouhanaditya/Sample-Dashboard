export default class Widget {
  constructor (id, name, size, backgroundColor, visible, type) {
    this.id = id
    this.name = name
    this.size = size
    this.backgroundColor = backgroundColor
    this.visible = visible
    this.type = type
  }
}
